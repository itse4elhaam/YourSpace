import { Metadata } from "next";

export const metadata: Metadata = {
	title: "YourSpace Blog section",
	description: "we're a social media app",
};

export default function Blog() {
	return <div>Blog</div>;
}
